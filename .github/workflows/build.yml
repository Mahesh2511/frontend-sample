# Bypass attempt for exercise D9: no call to the shared framework.
name: Build

on:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "skipping the shared PR checks"
